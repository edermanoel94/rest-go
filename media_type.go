package rest

// Headers keys
const (
	contentType = "Content-Type"
	location    = "Location"
)

// Headers values
const (
	applicationJson     = "application/json"
	applicationJsonUtf8 = applicationJson + ";charset=UTF-8"
)
